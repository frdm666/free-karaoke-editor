# Songs and everything the program makes on its own
projects/
music/
*.mp4
*_karaoke.html

karaoke.zip

# The interpreter the setup installed into — this machine's, not the repo's
app/.python-path

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Tests
# Without the slashless forms a symlink named node_modules is not ignored —
# and one of those, pointing into a temporary folder, spent the whole first
# release inside the repository.
app/node_modules
node_modules
app/node_modules/
node_modules/
package-lock.json

# Bits and pieces from the systems
.DS_Store
Thumbs.db
desktop.ini

# Your own settings: app/settings.example.ini is the reference, the copy is yours
app/settings.ini
